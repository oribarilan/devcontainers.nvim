{
  "name": "nvim-with-bob",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "set -eux; apt-get update && apt-get install -y curl ca-certificates unzip build-essential pkg-config; curl -fsSL https://sh.rustup.rs | sh -s -- -y; export PATH=$HOME/.cargo/bin:$PATH; cargo install bob-nvim; bob --version || true",
  "remoteEnv": {
    "PATH": "${containerEnv:HOME}/.cargo/bin:${containerEnv:HOME}/.local/share/bob/nvim-bin:${containerEnv:PATH}"
  }
}